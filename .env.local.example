# Copy to .env.local and fill in your values.

# Firebase (client) — use NEXT_PUBLIC_ so they're available in the browser.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Firebase Admin SDK (server-side only; do NOT prefix with NEXT_PUBLIC_).
# Get these from Firebase Console → Project settings → Service accounts → Generate new private key.
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
# Private key from the JSON; use actual newlines or \n (escape in .env as needed).
FIREBASE_ADMIN_PRIVATE_KEY=

# Optional: when no Firebase Auth is used, this user id is used for all data (e.g. "demo").
# Once you add Auth, remove this and use the signed-in user's uid.
FIREBASE_DEMO_USER_ID=demo

# Cloudinary (template logos). From Cloudinary Dashboard → Settings → API Keys.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
